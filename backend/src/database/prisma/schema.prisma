// Full DB schema (all models) — populated module-by-module starting with
// Module 02 (Authentication). Intentionally empty at scaffold time.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
